**What is a markup language?**
->   A markup language is a system for annotating a document with tags to define its structure, formatting, and relationship between parts

**Full name:**
  -> Previously-> Yet another markup language
  -> Present ->  Ymal ain't markup language (Because it stores documents as well as  objects data)

It is not a programming language . It is an data format used to exhange data. It is similar to xml & json data types.
It is basically a simple human readble language that is used to represnnt data.

In ymal you can store only data not commands.

It is known as storing data in files but in a structured way.

**Serialization:** It is a process to convert data objects into complex data structure.


**Data object** -> Stream (bytes stream/ chuncks of data)-> database (computer) this is known as serialization
database(computer)->Stream->object data this is known as deserialization.


**Object:** A object is a combinationn of code and data.

**Data serialization Languages**
1. Yaml
2. Json
3. Xml 

What type of files store in yml files: Configuration files. (Like Docker, kubernates files)

Where is it use: Logs, Caches ets

Benifits of yml:

1. Very easy to read human readble
2. It has a nice strict syntax. Here intentation in imp
3. It is easly convertable like in json or xml
4. Most languages use yml.
5. More powerfull when representing complex data.
6. Use variable tools with it.

In ymal you can store in key value pairs. In Block pairs. In List pairs


Key -> Value


**Practice::**
Link: https://www.yamllint.com/


In Ymal to reconize different types of documents we need to use --- 

In ymal to reconize the end of a documents we use ...